let circulo = document.querySelector(".circulo");
let imagem = document.querySelector(".starbuks");

function trocarCor(cor) {
  circulo.style.background = cor;
}

function trocarImagem(img) {
  imagem.src = img;
}
